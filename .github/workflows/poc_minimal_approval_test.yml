name: PoC Minimal Approval Test
on:
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: marker
        run: |
          echo "POC_APPROVAL_BYPASS_MARKER_1772013595"
          echo "## POC_APPROVAL_BYPASS_MARKER_1772013595" >> "$GITHUB_STEP_SUMMARY"
